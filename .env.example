<<<<<<< HEAD
# ---- Runtime ----
NODE_ENV=development
PORT=8080
LOG_LEVEL=info

# ---- CORS ----
# Comma-separated list of allowed origins for the API.
CORS_ORIGINS=http://localhost:3000

# ---- Rate limiting ----
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=300

# ---- Supabase ----
# Project URL (https://<ref>.supabase.co)
SUPABASE_URL=
# anon/public key — safe to expose but we still keep it server-side.
SUPABASE_ANON_KEY=
# Service-role key — NEVER expose to clients. Used for privileged server ops.
SUPABASE_SERVICE_ROLE_KEY=
# JWT secret used to verify access tokens issued by Supabase Auth.
SUPABASE_JWT_SECRET=

# ---- ScrapeCreators ----
SCRAPECREATORS_BASE_URL=https://api.scrapecreators.com
SCRAPECREATORS_API_KEY=
# Seconds to keep cached scrape responses before re-fetching.
SCRAPECREATORS_CACHE_TTL_SECONDS=86400

# ---- Audit log ----
# HMAC secret used to seed the audit log hash chain. Rotate via key versioning.
AUDIT_HMAC_SECRET=

# ---- KYC / PII encryption ----
# 32-byte key (hex-encoded, 64 chars) used for AES-256-GCM application-layer
# encryption of KYC fields. Rotate via envelope encryption.
KYC_ENCRYPTION_KEY=

# ---- Jobs ----
JOBS_ENABLED=true
JOBS_METRICS_REFRESH_CRON=0 3 * * *
JOBS_KYC_EXPIRY_CRON=0 4 * * *
JOBS_AUDIT_VERIFY_CRON=0 5 * * *

# ---- Observability (optional) ----
SENTRY_DSN=
=======
SCRAPECREATORS_API_KEY=your_scrapecreators_api_key_here
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here
CORS_ALLOWED_ORIGINS=http://localhost:3000
PORT=3001
LOG_LEVEL=info

# ---------------------------------------------------------------------------
# JWT Key Provider configuration
# ---------------------------------------------------------------------------
# KEY_PROVIDER selects how the JWT signing secret is obtained.
# Supported values: env | aws-kms | aws-secrets-manager
# Default: env
KEY_PROVIDER=env

# --- Provider: env (default, development only) ---
SUPABASE_JWT_SECRET=your_supabase_jwt_secret_here

# --- Provider: aws-kms (envelope encryption) ---
# KMS_KEY_ID=arn:aws:kms:us-east-1:123456789012:key/abcd-1234
# KMS_ENCRYPTED_SECRET=<base64-encoded ciphertext>
# AWS_REGION=us-east-1

# --- Provider: aws-secrets-manager ---
# AWS_SECRET_ARN=arn:aws:secretsmanager:us-east-1:123456789012:secret:my-jwt-secret
# AWS_SECRET_KEY=jwt_secret          # optional JSON key within the secret
# AWS_REGION=us-east-1

# ---------------------------------------------------------------------------
# Column-level PII field encryption (optional)
# ---------------------------------------------------------------------------
# When set, PII fields (full_name, bio, profile_pic_url, profile_url) are
# encrypted at rest using AES-256-GCM with envelope encryption via AWS KMS.
# Omit to disable field-level encryption (development / non-sensitive data).
# FIELD_ENCRYPTION_KMS_KEY_ID=arn:aws:kms:us-east-1:123456789012:key/abcd-1234
# AWS_REGION=us-east-1
>>>>>>> 17ef3c073da08a2589cd477774c945045b4ff8fd
